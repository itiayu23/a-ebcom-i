<div class="container mt-5">
        <div class="row">
          <div class="col">

<h1>小説投稿ページ</h1>
<% if user_signed_in? %>
<%= render 'layouts/errors', obj: @novel_new %>
<%= form_with model: @novel_new do |f| %>

<table class="table">

<div class="form-group">
  <%= f.text_field :title, placeholder: "タイトル", class: 'form-control' %>
</div>
  <!--別ページにリロードなしで行けて個別に書ける、下書き保存が出来る-->
  <div class="form-group">
  <%= f.text_area :text_body, placeholder: "本文", class: 'form-control' %>
  </div>

 <div class="form-group">
  <%= f.text_area :caption, placeholder: "小説の説明、あらすじなど", class: 'form-control' %>
 </div>
  <!--表紙アップロードにする。-->
  <div class="form-group">
    <%= f.label :image, "表紙画像アップロード" %><br>
    <%= f.file_field :image, placeholder: "小説の表紙", accept: "image/*", class: "form-control-file" %>
  </div>

  <div class="form-group">
    <%= f.select :privacy, [["公開",1],["非公開",2]], include_blank: "選択してください" %>
  </div>

  <!--タグの部分のみ抜粋-->
  <div class="form-group">
    タグをつける。複数つけるには','で区切ってください。
  <%= f.text_field :tag_ids, class: "form-control", id: 'tag_ids',\
   placeholder: "タグをつける。複数つけるには','で区切ってください。" %>
  </div>
  
  <div class="submit">
  <%= f.submit '小説を投稿する', name: "create" %>
  </div>
  
  <br>
<%= link_to "投稿の際はこちらを守っていただけますようお願いいたします。", homes_warning_path, class: "text-danger" %>

</table>
<% end %>
<% else %>
             <p>
                <%= link_to "投稿をするには新規登録をお願いします", new_user_registration_path, class: "text text-warning" %>
             </p>
             <p>
                <%= link_to "登録済みの方はこちらからログインをお願いいたします。", new_user_session_path %>
             </p>
              
<% end %>

</div>


  </div>
</div>
